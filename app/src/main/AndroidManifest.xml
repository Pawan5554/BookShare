<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.theflash.hpw.bookshare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main2" />
        <activity android:name=".profile" />
        <activity android:name=".chat" />
        <activity android:name=".addbook" />
        <activity
            android:name=".seerequest"
            android:label="@string/title_activity_seerequest" />
        <activity android:name=".ViewProfile" />
        <activity android:name=".search" />
        <activity android:name=".seemyfollower" />
        <activity android:name=".updateprofile"></activity>
    </application>

</manifest>